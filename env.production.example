# =============================================================================
# C++ Graph-RAG MCP Server - Production Environment Configuration
# =============================================================================
# This configuration uses pre-built Docker images from Docker Hub
#
# Usage:
#   1. Copy this file to .env
#   2. Customize the values below
#   3. Run: docker-compose -f docker-compose.production.yml up -d
#      Or: podman-compose -f docker-compose.production.yml up -d
# =============================================================================

# -----------------------------------------------------------------------------
# Docker Image Configuration
# -----------------------------------------------------------------------------
# Specify the Docker Hub image to use
# Format: username/repository:tag
#
# Examples:
#   DOCKER_IMAGE=johndoe/cpp-graph-rag-mcp:latest
#   DOCKER_IMAGE=johndoe/cpp-graph-rag-mcp:1.0.0
#
DOCKER_IMAGE=YOUR_DOCKERHUB_USERNAME/cpp-graph-rag-mcp:latest

# -----------------------------------------------------------------------------
# Host Path Configuration
# -----------------------------------------------------------------------------
# Mount your source code here. The web UI will let you select specific 
# subdirectories to index.
#
# Windows examples:
#   HOST_PATH=C:/
#   HOST_PATH=C:/Projects
#   HOST_PATH=D:/SourceCode
#
# Linux/Mac examples:
#   HOST_PATH=/home
#   HOST_PATH=/mnt/code
#   HOST_PATH=/Users/username/projects
#
# IMPORTANT: For Docker Desktop on Windows/Mac, ensure the drive is shared
# in Docker Desktop settings under: Settings > Resources > File Sharing
#
HOST_PATH=C:/Projects

# -----------------------------------------------------------------------------
# Monitored Paths (Optional)
# -----------------------------------------------------------------------------
# Comma-separated list of paths to monitor (relative to /host in container)
# If empty, configure via web UI at http://localhost:8000
#
# Examples:
#   MONITORED_PATHS=/host/myproject/src,/host/myproject/include
#   MONITORED_PATHS=/host/Projects/MyApp/Source,/host/Projects/MyApp/Include
#
# Leave empty to configure via web UI:
MONITORED_PATHS=

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection settings
# Default values work for local development
#
DB_NAME=cpp_codebase
DB_USER=postgres
DB_PASSWORD=postgres

# External port for PostgreSQL (useful if you want to connect directly)
# Set to empty to disable external access
# WARNING: In production, use strong passwords and firewall rules
DB_PORT_EXTERNAL=5432

# -----------------------------------------------------------------------------
# MCP Server Configuration
# -----------------------------------------------------------------------------
# Port for the MCP API and Web UI
MCP_PORT=8000

# Embedding model for semantic search
# Options:
#   sentence-transformers/all-MiniLM-L6-v2  (fast, 384 dimensions, ~90MB)
#   sentence-transformers/all-mpnet-base-v2 (better quality, 768 dimensions, ~420MB)
#
# The model is pre-downloaded in the Docker image, so changing this
# will require downloading a new model on first run (~2 minutes)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# -----------------------------------------------------------------------------
# Advanced Configuration (usually not needed)
# -----------------------------------------------------------------------------
# These are set automatically but can be overridden if needed

# Database host (container name in docker network)
# DB_HOST=postgres

# Database port (internal)
# DB_PORT=5432

# Config path for persistent settings
# CONFIG_PATH=/app/config
