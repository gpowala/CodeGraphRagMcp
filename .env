# =============================================================================
# C++ Graph-RAG MCP Server - Environment Configuration
# =============================================================================
# Copy this file to .env and customize for your environment
#
# Usage:
#   Docker:  docker-compose up -d
#   Podman:  podman-compose up -d
# =============================================================================

# -----------------------------------------------------------------------------
# Host Path Configuration
# -----------------------------------------------------------------------------
# Mount your source code here. The web UI will let you select specific 
# subdirectories to index.
#
# Windows examples:
#   HOST_PATH=C:/
#   HOST_PATH=D:/Projects
#
# Linux/Mac examples:
#   HOST_PATH=/home
#   HOST_PATH=/mnt/code
#
HOST_PATH=./example_code

# -----------------------------------------------------------------------------
# Monitored Paths (Optional)
# -----------------------------------------------------------------------------
# Comma-separated list of paths to monitor (relative to /host in container)
# If empty, configure via web UI at http://localhost:8000
#
# Examples:
#   MONITORED_PATHS=/host/myproject/src,/host/myproject/include
#   MONITORED_PATHS=/host/Projects/MyApp/Source
#
MONITORED_PATHS=

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection settings
# Default values work for local development
#
DB_NAME=cpp_codebase
DB_USER=postgres
DB_PASSWORD=postgres

# External port for PostgreSQL (useful if you want to connect directly)
# Set to empty to disable external access
DB_PORT_EXTERNAL=5432

# -----------------------------------------------------------------------------
# MCP Server Configuration
# -----------------------------------------------------------------------------
# Port for the MCP API and Web UI
MCP_PORT=8000

# Embedding model for semantic search
# Options:
#   sentence-transformers/all-MiniLM-L6-v2  (fast, 384 dimensions)
#   sentence-transformers/all-mpnet-base-v2 (better quality, 768 dimensions)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# -----------------------------------------------------------------------------
# Advanced Configuration (usually not needed)
# -----------------------------------------------------------------------------
# These are set automatically but can be overridden if needed

# Database host (container name in docker network)
# DB_HOST=postgres

# Database port (internal)
# DB_PORT=5432

# Config path for persistent settings
# CONFIG_PATH=/app/config
